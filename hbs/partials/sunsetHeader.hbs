{{~# if true ~}}
'{{# if sunsets ~}}
299 - Deprecated: {{ version ~}}
; Service: {{ service ~}}
; Sunset: {{ UTCDate (lookup sunsets version) ~}}
{{~# ifInKeys recommended upstreams }}; Recommended: {{recommended ~}}{{/ifInKeys~}}
{{~/if ~}}'
{{~/if ~}}
