{{# if upstreams ~}}
~*(application/vnd\.
  {{~#if namespace ~}}
    {{ namespace }}
  {{~ else ~}}
    \S+
  {{~/if~}}
  \.v|version=v?)(?!(
    {{~ versionsJoinWith upstreams '|' ~}}
  )\b)
{{~/if }}
